import React from 'react';

const Loader = () => {
  return (
    <div>
      <div class="spinner-border spinnerLoader" role="status">
        <span class="sr-only">Loading...</span>
      </div>
    </div>
  );
};

export default Loader;
